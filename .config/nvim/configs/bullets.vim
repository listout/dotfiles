" filetypes to enable bullets
let g:bullets_enabled_file_types = [
			\ 'markdown',
			\ 'text',
			\ 'gitcommit',
			\ 'scratch'
			\]

" disable for empty buffers
let g:bullets_enable_in_empty_buffers = 0

" delete last bullets if empty
let g:bullets_delete_last_bullet_if_empty = 1

" don't add extra padding between the bullets
let g:bullets_pad_right = 0
